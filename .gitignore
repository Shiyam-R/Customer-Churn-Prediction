venv/
env/
__pycache__/
*.pyc
*.pyo
.env
.ipynb_checkpoints/
*.log
.DS_Store
.vscode/
.idea/

# NOTE: artifacts/ (churn_model.pkl, model_columns.json) is intentionally
# NOT ignored — committed directly per the training/serving separation
# decision. Small, stable, doesn't change on every commit.
